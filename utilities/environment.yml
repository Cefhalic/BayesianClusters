channels:
  - conda-forge
  - pwwang
dependencies:
      - cxx-compiler
      - glibc214
      - boost=1.80.0
      - fmt
      - gsl
      - make
      - lld=15.0.5
      - python
      - ipykernel 
      - jupyter_client
      - pip
      - pip:
        - matplotlib
